#include <stdio.h>
#include <stdlib.h>

/**
 * main - main - print all letters of the alphabeth in lower case
 * 
 * Return: 0 if successful
 */
int main(void)
{
	cha var;
        cha var1;

	for (var = 'a'; var <= 'z'; var++)
	{
		putchar(c);
	}

        for (var1 = 'A'; var1 <= 'Z'; var1++)
	{
	        putchar(c);
	}
	putchar('\n');
	return (0);
}
